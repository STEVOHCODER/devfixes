# DevFixes Error Search

Select an error message or stack trace in VS Code, then run:

- `DevFixes: Search Selected Error`
- `DevFixes: Debug Selected Error`

The extension opens the selected text on `https://devfixes.vercel.app`. It does
not collect telemetry, read workspace files, or require an API key.
